spring.application.name=acn-bootcamp-app

# Port configuration - uses SERVER_PORT environment variable or defaults to 3300
server.port=${SERVER_PORT:3300}

# Thymeleaf configuration
spring.thymeleaf.cache=false
